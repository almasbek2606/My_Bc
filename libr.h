#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
#include <stdlib.h>
#include <stdbool.h>

#define WEIGHT 256
int floating[WEIGHT]; 
int dev[WEIGHT];
/*void get_edit(int);
int print_of_error();
int add_mult_(char);
int its_wrong();
int top_element();
void infix_to_postfix(char*, char*);
void search_error(int);
int else_err();
int character_of_set(char);
void sort_of_input_things(char*);
int search_position(char);
int search_index(char);
int is_b(char);
int is_avaiable_input(char*);
*/